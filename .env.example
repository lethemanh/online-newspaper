# Example environment variables for Online Newspaper Project

# Database provider: "SqlServer" or "Sqlite"
DB_PROVIDER=SqlServer

# SQL Server connection string (used if DB_PROVIDER=SqlServer)
# Replace 'YourStrong!Passw0rd' with your actual SQL Server password
SQLSERVER_CONNECTION_STRING=Server=localhost;Database=BaoDienTu;User Id=sa;Password=YourStrong!Passw0rd;TrustServerCertificate=True;

# SQLite connection string (used if DB_PROVIDER=Sqlite)
SQLITE_CONNECTION_STRING=Data Source=BaoDienTu.db

# ASP.NET Core service port
ASPNETCORE_URLS=http://localhost:5046
